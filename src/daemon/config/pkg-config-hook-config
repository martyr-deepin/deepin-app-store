post-invoke=if { test "$DPKG_HOOK_ACTION" = install || test "$DPKG_HOOK_ACTION" = remove || test "$DPKG_HOOK_ACTION" = unpack || test "$DPKG_HOOK_ACTION" = purge; } && test -x /usr/share/pkg-config-dpkghook; then /usr/bin/deepin-app-store-pkgcache -u; fi
